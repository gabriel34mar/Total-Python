"""Create a function called number_attributes that counts the number of parameters that are passed, and returns that number as the result."""
def number_attributes(**numbers):
    result=len(numbers)
    return result
    